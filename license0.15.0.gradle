apply plugin: 'com.github.hierynomus.license-base'

license {
    header = file("${rootProject.projectDir}/HEADER_LICENSE.txt")
    strictCheck true

    mapping {
        java='SLASHSTAR_STYLE'
    }
}
